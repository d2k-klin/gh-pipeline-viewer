{
  "_comment": "Your dashboard. 'branches' and 'workflows' apply to every repo; any repo can override either. Empty list means all of them.",
  "branches": ["main", "master"],
  "workflows": [],
  "repos": [
    "actions/checkout",
    { "repo": "actions/runner", "branches": ["main"], "workflows": ["Runner CI"] }
  ]
}
