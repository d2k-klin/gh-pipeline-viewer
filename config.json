{
  "_comment": "Your dashboard. 'branches' applies to every repo; a repo can override it. Empty list means every branch.",
  "branches": ["main", "master"],
  "repos": [
    "actions/checkout",
    { "repo": "actions/runner", "branches": ["main"] }
  ]
}
